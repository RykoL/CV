{
	"name": "template-worker-rust",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --local",
		"test": "mocha integration"
	},
	"devDependencies": {
		"mocha": "^10.2.0",
		"wrangler": "^2.13.0"
	}
}
